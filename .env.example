APP_NAME=parse-agent
APP_ENV=dev
LOG_LEVEL=INFO

# LangChain 模型配置，后续接入 Agent 时填写
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=
PDF_NORMAL_PROVIDER=pdf.normal.pdf-inspector
PDF_FALLBACK_PROVIDERS=
# 扫描版 / 图片型 PDF：安装 `uv sync --group ocr` 后首次识别会自动下载 PaddleOCR 模型。
PDF_OCR_ENABLED=true
PDF_OCR_PROVIDER=pdf.ocr.paddle
PDF_OCR_DEVICE=cpu
PDF_OCR_LANG=ch
PDF_OCR_MAX_PAGES=50
PDF_OCR_RENDER_SCALE=2.0
PDF_OCR_MAX_PAGE_PIXELS=20000000
# 独立图片 OCR：与 PDF OCR 共用 PaddleOCR 依赖，可单独启用和限制输入像素。
IMAGE_OCR_ENABLED=true
IMAGE_OCR_PROVIDER=image.ocr.paddle
IMAGE_OCR_DEVICE=cpu
IMAGE_OCR_LANG=ch
IMAGE_OCR_MAX_PIXELS=20000000
WORD_NORMAL_PROVIDER=word.normal.mammoth
WORD_FALLBACK_PROVIDERS=
OFFICE_CONVERTER_PROVIDER=office.convert.libreoffice
OFFICE_CONVERTER_COMMAND=soffice
OFFICE_CONVERTER_TIMEOUT_SECONDS=300

# 媒体预处理：仅探测、音轨提取和标准化；不执行 ASR。
MEDIA_PREPARE_PROVIDER=media.prepare.ffmpeg
MEDIA_FFMPEG_COMMAND=ffmpeg
MEDIA_FFPROBE_COMMAND=ffprobe
MEDIA_TIMEOUT_SECONDS=300
MEDIA_MAX_DURATION_SECONDS=14400
MEDIA_MAX_FILE_SIZE_MB=2048

EXCEL_NORMAL_PROVIDER=excel.normal.openpyxl
EXCEL_MAX_SHEETS=30
EXCEL_MAX_SEMANTIC_CELLS=1000000
EXCEL_MAX_SHEET_ROWS=100000
EXCEL_MAX_COLUMNS=1000
EXCEL_MAX_FILE_SIZE_MB=100
EXCEL_MAX_UNCOMPRESSED_SIZE_MB=500
EXCEL_MAX_ZIP_ENTRIES=10000
EXCEL_MAX_SHEET_XML_SIZE_MB=100
PPT_NORMAL_PROVIDER=ppt.normal.python-pptx

# 单进程持久化任务队列：排队任务在重启后恢复，运行中任务会标记为 interrupted。
TASK_QUEUE_ENABLED=true
TASK_MAX_CONCURRENT_EXECUTIONS=2
TASK_QUEUE_MAX_SIZE=100
TASK_DEFAULT_TIMEOUT_SECONDS=1800
TASK_RESULT_TTL_SECONDS=86400
TASK_CLEANUP_INTERVAL_SECONDS=300
TASK_SHUTDOWN_GRACE_SECONDS=30
TASK_MAX_RESULT_SIZE_MB=20
STORAGE_MIN_FREE_MB=1024
STORAGE_SOFT_FREE_MB=2048
STORAGE_TMP_MAX_AGE_SECONDS=86400

# 受控本地文件、任务和 artifact 存储。
DATA_DIR=./data
FILE_STORAGE_DIR=./data/files
FILE_MAX_SIZE_MB=100
FILE_ALLOWED_SUFFIXES=.pdf,.doc,.docx,.xls,.xlsx,.xlsm,.ppt,.pptx,.txt,.md,.markdown,.csv,.tsv,.html,.htm,.xml,.rtf,.json,.yaml,.yml,.ini,.cfg,.conf,.log,.sql,.js,.ts,.css,.png,.jpg,.jpeg,.webp,.bmp,.tif,.tiff,.mp3,.wav,.m4a,.aac,.flac,.ogg,.mp4,.mov,.mkv,.avi,.webm
# 留空则不启用 REST API Key 鉴权。远程 MCP 启用时必须设置非空 API_KEY。
API_KEY=
# Streamable HTTP MCP endpoint at /mcp/. Keep disabled unless a remote MCP client needs it.
# Enabling it requires a non-empty API_KEY. Set the public Docker hostname here.
MCP_HTTP_ENABLED=false
MCP_ALLOWED_HOSTS=localhost,127.0.0.1,localhost:*,127.0.0.1:*,[::1]:*

TEXT_MAX_FILE_SIZE_MB=20
TEXT_MAX_TABLE_ROWS=100000
TEXT_MAX_TABLE_COLUMNS=1000
TEXT_MAX_XML_NODES=100000
TEXT_MAX_XML_DEPTH=128

# RTF 智能路由：auto=复杂 RTF 转 DOCX，direct=仅直接文本，convert=总是转 DOCX。
RTF_ROUTING_MODE=auto
RTF_DIRECT_MAX_COMPLEXITY_SCORE=5
RTF_DIRECT_MAX_SIZE_MB=2
RTF_DIRECT_MIN_TEXT_RATIO=0.01
